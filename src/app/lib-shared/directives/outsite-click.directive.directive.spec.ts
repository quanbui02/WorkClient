/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { OutsiteClick.directiveDirective } from './outsite-click.directive';

describe('Directive: OutsiteClick.directive', () => {
  it('should create an instance', () => {
    const directive = new OutsiteClick.directiveDirective();
    expect(directive).toBeTruthy();
  });
});
